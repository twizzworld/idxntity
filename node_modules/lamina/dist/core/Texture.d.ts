import { IUniform, Texture } from 'three';
import { BlendMode, TextureProps } from '../types';
import Abstract from './Abstract';
export default class Noise extends Abstract {
    name: string;
    mode: BlendMode;
    protected uuid: string;
    uniforms: {
        [key: string]: IUniform<any>;
    };
    constructor(props?: TextureProps);
    getVertexVariables(): string;
    getVertexBody(e: string): string;
    getFragmentVariables(): string;
    getFragmentBody(e: string): string;
    set alpha(v: number);
    get alpha(): number;
    set map(v: Texture);
    get map(): Texture;
}
