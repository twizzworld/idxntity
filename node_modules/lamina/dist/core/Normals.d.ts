import { ColorRepresentation, IUniform, Vector3 } from 'three';
import { BlendMode, NormalsProps } from '../types';
import Abstract from './Abstract';
export default class Noise extends Abstract {
    name: string;
    mode: BlendMode;
    protected uuid: string;
    uniforms: {
        [key: string]: IUniform<any>;
    };
    constructor(props?: NormalsProps);
    getVertexVariables(): string;
    getVertexBody(e: string): string;
    getFragmentVariables(): string;
    getFragmentBody(e: string): string;
    set alpha(v: number);
    get alpha(): number;
    set color(v: ColorRepresentation);
    get color(): ColorRepresentation;
    set direction(v: Vector3);
    get direction(): Vector3;
}
